name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # molsystem requires, and only available via Conda.
  - openbabel
  - psutil

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - seamm

    # Testing
    - yapf

    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
