name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # Of the code
  - numpy
  - optking
  - psutil
  - scipy
  - seamm

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
