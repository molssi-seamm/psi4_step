name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # Of the code
  - numpy
  - optking
  - psutil
  - scipy
  - seamm
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    # Documentation
    - sphinx-copybutton
