name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # Of the code
  - cclib
  - numpy
  - optking
  - scipy
  - seamm
  - seamm-installer
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
      - seamm-exec
      # Documentation
      - sphinx-copybutton
